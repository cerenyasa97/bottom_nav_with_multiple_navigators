import 'package:flutter/material.dart';

class ExamplePage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Example Page 2"),
      ),
      body: Center(child: Text("Example page 2"),),
    );
  }
}
